export class CartItem {
  constructor(quantity, priceProduct, titleProduct, sum) {
    this.quantity = quantity;
    this.priceProduct = priceProduct;
    this.titleProduct = titleProduct;
    this.sum = sum;
  }
}
